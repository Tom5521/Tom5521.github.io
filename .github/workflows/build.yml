on: [push]

jobs:
  build:
    runs-on: fedora-latest
    steps:
      - name: build
        uses: ./.github/actions/build-img
      - name: copy
        run: mkdir -p js && cp /github/workspace/js/* ./js/
        with:
          name: imgs
          path: ${{ github.workspace }}
